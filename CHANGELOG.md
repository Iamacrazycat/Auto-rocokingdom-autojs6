# 更新日志

## [2026/05/20] - 攻击模式与战斗判定优化 (v1.2.0)

### 🛠️ 攻击模式上线
*   **支持自定义坐标攻击**：针对不同分辨率或多变的游戏界面，新增“攻击模式”。允许用户手动寻得坐标并录入 X、Y 坐标参数，解决攻击按钮坐标与图标不固定的问题。
*   **灵活的回合流程配置**：支持首回合点击自定义攻击，后续回合选择**“聚能”**或**“重复攻击”**。
*   **可捕捉状态动态响应**：战斗期间若发现捕捉按钮变为可用（红标消失，表示精灵已被击败），自动转为**“聚能”**行为，智能判断结束条件。

### 🔄 状态分发与修复
*   **修复模式选择被忽略的 Bug**：重构了 `bot.js` 中的模式分发逻辑，解决了之前无论在 UI 选择何种模式，启动后都默认只走“智能模式”的问题。现在系统严格遵循 聚能模式、逃跑模式、智能模式 和 攻击模式 的设定执行。
*   **多参数本地持久化**：新增的自定义 X、Y 坐标、后续动作以及可捕捉选项，在 App 退出重启后依然能够被完整保留。

---

## [2026/05/18] - UI/UX 体验跃升与性能持久化 (v1.1.0)

### 🎨 悬浮球与 UI/UX 革命
*   **极简贴边悬浮球**：重构了原先臃肿的方框悬浮窗，打造了半透明、高颜值的极简悬浮球。支持拖动智能贴边吸附，点击展开折叠式操作菜单（暂停/继续/关闭/收起）。
*   **动态颜色状态指示**：悬浮球背景颜色可根据 bot 运行状态智能切换（运行中为**生态绿**，已暂停为**警戒橙**），交互手感媲美 iOS/Android 系统级辅助触控。
*   **权限感应式反馈**：主界面的“无障碍服务”和“悬浮窗”按钮现已接入动态检测，自动通过红/绿背景实时显示“已开启/未开启”状态，且在 App 每次切回前台（`resume` 事件）时自动刷新。

### 📉 坐标持久化与性能极致优化
*   **本地存储持久化**：将 `skill_x` (技能按钮)、`escape_btn` (逃跑) 和 `escape_yes` (逃跑确认) 的模板匹配坐标从内存缓存升级为**本地持久化存储 (`storage`)**。在后续重新运行脚本时，直接读取历史匹配坐标实现“闪电点击”，匹配计算频率降至 0。
*   **一键缓存重置**：为应对屏幕 180 度反转或刘海屏引起的物理坐标偏移，主界面新增了 **【清除坐标缓存】** 一键重置功能，允许用户随时清除本地缓存以重新触发 OpenCV 模板高精度识别。

### 🛡️ 稳定性与体验修复 (Bug Fixes)
*   **销毁屏幕录制残留**：解决了退出脚本后由于进程未完全释放，导致**系统顶部屏幕录制/截屏监听图标依然残留**的问题。现在点击【关闭】时会彻底退出脚本引擎释放录屏权限。
*   **逃跑缓冲时延**：优化了两次逃跑点击的流程，在点击“逃跑”和寻找“确认(是)”之间强行引入了 `1500ms` 的渲染缓冲延迟，彻底消除了由于动画未加载完毕导致漏点确认按钮的异常。
*   **清理多余配置项**：彻底剔除了主界面无实际作用的“基准屏幕宽度/高度”硬编码参数，简化了用户配置流程。

### 🧱 代码解耦与模块化
*   **主脚本彻底瘦身**：将原先在 `main.js` 中纠缠不清的百余行悬浮窗 UI 定义、交互拖拽和按钮回调代码，完美剥离并封装至独立的 `src/floaty_manager.js` 中，大幅缩减了主脚本行数，系统层次更清晰，可维护性大幅提高。

---

## [2026/05/16] - 架构革新与交互优化

### 🚀 核心改进
*   **架构重构**：采用**观察者模式**配合**状态机**。通过实时匹配屏幕状态（非战斗/有效战斗/无效战斗/其他），并引入“状态向上追溯”逻辑，彻底解决了战斗切换过程中的状态抖动与识别不稳定问题。
*   **视觉算法升级**：摒弃了旧版本的 OCR 模式，转而使用全 **OpenCV 模板匹配**。针对高度相似的 `capture` 和 `capture_abandon` 界面，引入了 **HSV Marker 过滤算法**（参考 Python 视觉分析脚本），大幅拉开了相似图像的匹配得分。

### 🎨 交互与 UI
*   **原生 UI 面板**：新增可视化设置界面，支持一键引导用户开启“无障碍”与“悬浮窗”权限。
*   **本地存储持久化**：配置项与 Auto.js `storages` 模块绑定，用户修改的参数（轮询间隔、匹配阈值、分辨率基准等）可持久化保存，下次启动自动加载。
*   **交互式悬浮窗**：自定义悬浮面板取代传统日志台。支持：
    *   **任意拖拽**：不遮挡游戏视线。
    *   **运行/暂停切换**：随时暂停脚本以进行手动干预，恢复后无缝衔接状态机逻辑。
    *   **一键退出**：快速销毁线程。

### 📉 性能与适配
*   **坐标缓存机制**：针对 `skill_x` 和 `escape_btn` 等固定位置按钮实现“首次匹配即缓存”，避免每一轮都进行全量图像匹配，显著降低 CPU 占用。
*   **多比例缩放适配**：更新参考分辨率为 `2772 * 1280`，并优化了多缩放比（长/宽/原始比例）的遍历匹配逻辑。
*   **解绑系统按键**：禁用了 Auto.js6 默认的音量下键停止脚本机制，防止误触。

---

## [2026/04/19] - 初始 OCR 版本
*   **全面转向 OCR 识别**：采用 mlkit OCR 替代 OpenCV 兼容性较差的算法。
*   **响应式点击**：识别文字坐标并计算图标相对偏移。
*   **智能判定**：基于关键字库命中逻辑进行战斗状态确认。
